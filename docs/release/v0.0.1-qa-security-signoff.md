# v0.0.1 QA & Security Sign-off

Jira: FORNX-238. Date: 2026-08-30. Version: 1.

This is the consolidated sign-off record required by FORNX-238's acceptance
criteria. It does not repeat evidence already recorded elsewhere in Jira —
it cites it — and it adds the one genuinely-missing exercise identified in
the 2026-08-30 reconciliation comment on FORNX-238: a formal command/path
injection, XSS, and redaction-egress negative-test campaign.

## 1. Frozen candidate

Matches `release/v0.0.1-candidate-manifest.json` as of this document's
commit.

| Repo | Owner | SHA | Publishes artifact |
|---|---|---|---|
| fornax-core | horonomy | `1c078ed31c23ffac8f515e8a46c97c1888c76457` | yes |
| fornax-cloud | horonomy | `84f02a0c8c0a14ca64176c22799ba9f4b50c1b4f` | yes |
| fornax-infra | horonomy | `13f17453776dcfa63971e18b2b64bec4aa621abc` | no |
| fornax-docs | horonomy | `d7c42731c16b27caf2149817263ba78969ab5937` | no |
| fornax-website | horonomy | `e351cd579adc9d55362ee15a7b07c735223c9f74` | no |

**Freeze note:** `fornax-core`'s SHA was updated from the FORNX-234-era
manifest value (`d22721b`) to `1c078ed` — current `main` — because FORNX-235
(release-execute.sh) merged after the FORNX-234 candidate manifest was last
written. The other four repos' `main` already matched the manifest exactly
and were left untouched.

This document itself, and the FORNX-279 tooling fix below, land on
`fornax-core` `main` *after* `1c078ed`. Neither changes any shipped binary
or runtime behavior — they are release-process/documentation-only. The
manifest is intentionally **not** bumped to chase them: `release-readiness.sh`'s
`sha_on_main` check accepts `identical` *or* `behind`, precisely so that
process commits landing after a freeze don't require re-freezing. No other
commits were made to any of the five repos' `main` branches during this
pass.

## 2. QA evidence (cited, not re-run)

All of the following were produced organically this session against this
same local-first MVP scope and are not repeated here:

| Ticket | Status | What it covers |
|---|---|---|
| FORNX-34 | Done | End-to-end local-first path proof across Claude Code and Codex, cloud disabled — fresh clone of all three repos, fresh-checkout Quick Start reproduction, full local cloud-shaped stack walkthrough |
| FORNX-53 | Done | Claude Code adapter Capabilities + real transcript field correctness |
| FORNX-55 | Done | Codex adapter event-shape correction against installed CLI (0.147.0) |
| FORNX-57 | Done | Store WAL/SHM sidecar file permissions hardening |
| FORNX-62 | Done | export-spool Capabilities envelope persistence |
| FORNX-44 | Done | Website minimal product/pricing/OSS surface |
| FORNX-45 | Done | Docusaurus technical site build |
| FORNX-39 | Done | Cloud ingest API + Pub/Sub handoff |
| FORNX-40 | Done | Cloud backend FastAPI/SQLModel/Alembic |
| FORNX-41 | Done | Cloud sync batch/retry/upload path |
| FORNX-42 | Done | SaaS Evidence dashboard (React frontend) |
| FORNX-61 | Done | Cloud-side verifier worker |
| FORNX-54 | **In Review, not Done** | One-command full-stack dev orchestration (Docker Compose + `just`) |

FORNX-54 is listed in FORNX-238's own reconciliation comment as covered
evidence, but as of this sign-off its ticket status is **In Review**, not
Done. Recorded accurately rather than rounded up — it does not block this
sign-off (it is developer-experience tooling, not a P0 user journey or a
security control), but it should not be read as closed QA evidence.

## 3. Security evidence

### 3.1 Dependency / supply-chain (cited from FORNX-238's 2026-08-30 comment, not re-run)

- `cargo audit` fornax-core: 1 finding (RUSTSEC-2023-0071, rsa crate,
  medium, timing side-channel) — pulled in only via sqlx's optional
  `mysql` feature, which this workspace does not enable. Accepted risk,
  not exploitable in this build.
- `cargo audit` fornax-cloud: 0 findings.
- `npm audit` fornax-cloud/frontend, fornax-website: 0 findings.
- `npm audit` fornax-docs: 24 findings (18 high), all transitive through
  Docusaurus 3.10.2's dev-server dependency chain; affects local dev
  server only, not the published static site. No clean fix available
  without a Docusaurus major bump. Documented, not exploitable in the
  shipped artifact.

### 3.2 Command / path injection campaign (new, this pass)

**Method:** static source review of every crate that touches
attacker-controlled input (adapter `tool_input`/`command` fields, session
IDs, event payloads), since standing up the full live stack's daemon,
uploader, and ingest processes to fuzz them with shell metacharacters was
not necessary once the actual attack surface was enumerated: there is no
shell-invocation surface for untrusted data in either repo's production
code to fuzz in the first place.

- `crates/` in fornax-core (`fornax-adapter-claude`, `fornax-adapter-codex`,
  `fornax-daemon`, `fornax-store`, `fornax-cli`, `fornax-verify`,
  `fornax-types`): **zero** occurrences of `std::process::Command`,
  `Command::new`, or any `sh -c` construction in production source. No
  process-spawning surface exists to inject into. **PASS by absence of
  attack surface**, stated explicitly rather than fabricating a test
  against a control that doesn't exist.
- fornax-cloud `crates/` and `backend/`: same result — the only
  `tokio::process::Command` usage in the entire repo is
  `crates/fornax-uploader/tests/real_backend.rs`, a test-only harness that
  shells out to `psql` for assertion setup, parameterized by a `Uuid`
  value (not an attacker-controlled string) — not production code, not
  reachable from any adversarial input. **PASS.**
- Path construction from attacker-controlled fields: traced every
  `PathBuf`/`.join(`/`fs::write`/`File::create` call in
  `crates/fornax-uploader/src/spool.rs` (the most plausible location, since
  it writes one file per queued message). Spool filenames are built from
  `Uuid::new_v4()` values generated internally by the spool itself, never
  from `session_id`, `tool_input`, or any other field in the ingested
  message. Grepped both repos for `session_id`/`event_id`/`tool_input`/
  `command` flowing into any path-construction call — no hits. **PASS**:
  no path-traversal surface (`../../etc/passwd`, null bytes, oversized
  payloads in these fields cannot reach a filesystem path).
- Malformed/adversarial *event* input (as opposed to path/command
  construction) — duplicate/unknown/malformed event handling — was already
  covered as part of FORNX-34's WAL durability and restart/recovery
  evidence; not repeated here.

**Not run:** no live fuzz/adversarial-payload run against a running
`fornax-daemon`/adapter process was performed in this pass. Given the
static result (no shell-invocation or attacker-influenced path-construction
surface exists anywhere in either repo), a dynamic run would be exercising
input validation and storage-as-inert-data behavior, not an actual
injection primitive. This is flagged as **NOT RUN, not PASS** — a future
pass with time budget for live fuzzing is legitimate defense-in-depth, but
no Critical/High risk is left open by skipping it, since there is nothing
in the code today for a crafted payload to reach.

### 3.3 XSS campaign (new, this pass)

**Method:** static review of the fornax-cloud React frontend for every
common unsafe-HTML sink, plus tracing how claim/rationale/evidence text
actually reaches the DOM.

- `grep -rn dangerouslySetInnerHTML frontend/src` — 0 hits.
- `innerHTML`, `insertAdjacentHTML`, `document.write`, `new Function(...)` —
  0 hits anywhere in `frontend/src`.
- No markdown-with-raw-HTML renderer (`react-markdown` + `rehype-raw`,
  `marked`, etc.) is a dependency (`frontend/package.json`).
- No `javascript:`-scheme URI construction in `frontend/src`.
- `FindingDetailPage.tsx` renders `data.claim_text` and `data.rationale` as
  plain JSX expression children (`<p>{data.claim_text}</p>`,
  `<p>{data.rationale}</p>`) — React's default text-node escaping applies;
  there is no sink anywhere in the codebase capable of interpreting that
  string as HTML/script.

**PASS by absence of sink**, stated explicitly per the same principle as
3.2: there is no code path in this frontend that can execute a string as
markup, so a payload like `<script>alert(1)</script>` in a claim or
rationale field can only ever render as literal text.

**Not run:** a live Playwright confirmation (ingest a claim containing
`<script>alert(1)</script>` end-to-end through the real backend and assert
it renders as inert text in a running frontend) was **not performed** in
this pass — bringing up the full backend/Postgres/frontend stack was out
of the available session time budget once the static result showed no
exploitable sink exists. Flagged as **NOT RUN, not PASS**. No Critical/High
risk is left open: the static sink enumeration is exhaustive for the
frontend's actual dependency set, not a sample.

### 3.4 Redaction-before-egress re-confirmation (FORNX-33/41 already-known-good, quick re-check this pass)

**Method:** ran the real unit test suite for the egress guard
(`crates/fornax-uploader/src/guard.rs`) in fornax-cloud, which is the
last-line-of-defense check described in that file's own module doc
("Defensive last-line-of-defense check against unredacted secrets... a
message that trips this check is never [sent] to the cloud"), against a
synthetic-secret fixture set already present in that file (env-style
secret assignments, GitHub-token-shaped strings, high-entropy strings), run
for real:

```
cd fornax-cloud && CARGO_TARGET_DIR=./target cargo test -p fornax-uploader --lib
```

Result: **14 passed, 0 failed** (this pass, 2026-08-30).

**Not run this pass:** the full live daemon → export-spool → uploader →
ingest → backend → Postgres pipeline test
(`crates/fornax-uploader/tests/real_backend.rs`) is marked
`#[ignore = "requires a live FORNX-40 backend + Postgres"]` and requires
`FORNAX_UPLOADER_TEST_REAL_DATABASE_URL` plus a running backend — neither
was stood up in this pass, so this specific ignored test was **NOT RUN**
here (it also covers idempotency, not redaction, per its own module doc).
The original FORNX-33/41 evidence already exercised redaction against a
live Postgres row and log output; that prior result is not superseded or
contradicted by anything found in this pass — it is simply not re-run
end-to-end here. Flagged as **NOT RUN (full E2E), PASS (unit-level
re-confirmation)**.

## 4. Checklist verdict (this ticket's own AC)

| AC item | Verdict | Basis |
|---|---|---|
| Stage/local readiness blockers relevant to v0.0.1 are closed or explicitly release-blocking | PASS | FORNX-34 Done, no open release-blocking item found for v0.0.1 scope |
| Candidate manifest and feature/coverage reconciliation are complete | PASS | `release/v0.0.1-candidate-manifest.json` (frozen SHAs above) + this document + FORNX-238's 2026-08-30 reconciliation comment |
| All P0 local user journeys pass against the frozen candidate | PASS | FORNX-34 fresh-checkout end-to-end walkthrough, cited above |
| Security sign-off is PASS with no unresolved Critical/High blocker | PASS | §3.1–3.4; zero Critical/High findings; two dependency findings accepted-risk and documented; two NOT RUN dynamic exercises with no open Critical/High risk implied |
| QA sign-off is PASS; blocked/untested surfaces are not hidden | PASS | §2 lists FORNX-54 accurately as In Review, not Done; §3.2/3.3/3.4 explicitly mark dynamic/E2E exercises as NOT RUN rather than rounding to PASS |
| FORNX-217 release docs/claims match actual candidate behavior | Not independently re-verified this pass — out of this ticket's touched scope; no claim in this document depends on it |
| Any candidate change after sign-off invalidates this result and requires affected re-verification | Acknowledged — see freeze note in §1 |

**Overall: PASS.** No unresolved Critical/High defect. One real tool defect
was found and fixed during this pass (not a product security defect): see
§5.

## 5. Defect found and fixed during this pass

**FORNX-279** (Bug): `scripts/release-readiness.sh`'s `not_blocked` gate
check used a case-insensitive substring match on `"block"`, which
false-positives on ordinary prose ("release-blocking", "blocker") present
in this very ticket's own description and in FORNX-34's closing comment —
making the `qa`/`security`/`stage` gates permanently unpassable for
well-written tickets, independent of any real BLOCK verdict. Fixed in
[fornax-core#23](https://github.com/horonomy/fornax-core/pull/23) with a
case-sensitive whole-word match on the literal token `BLOCK`, consistent
with the documented convention and the existing test fixture. This is a
release-tooling correctness defect, not a security or product defect, and
does not gate this sign-off's PASS verdict — it is disclosed here because
it directly affects whether `release-readiness.sh` can evaluate this very
ticket correctly.

## 6. Scope explicitly not covered

Per the owner's scoping of this pass to FORNX-234/235/238/279 only:
FORNX-230 (QA gate) and FORNX-233 (Security gate) do not exist as separate
tickets and are not created or blocked on here — this document and
FORNX-238's own AC checklist serve as the actual gate for this candidate.
